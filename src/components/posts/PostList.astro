---
import PostCard from "./PostCard.astro";
import type { LogEntry } from "../../utils/posts";

interface Props {
  posts: LogEntry[];
}

const { posts } = Astro.props as Props;
---

<div class="post-list">
  {posts.map((entry, index) => <PostCard entry={entry} index={index + 1} />)}
</div>
